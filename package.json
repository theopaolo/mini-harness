{
  "name": "titi",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "debug": "bun run index.ts --debug --step",
    "benchmark": "bun run src/benchmarks/general.ts",
    "tool-benchmark": "bun run src/benchmarks/toolCalling.ts",
    "benchmarks:refresh": "bun run src/routing/refreshBenchmarks.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test --pass-with-no-tests"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "parse5": "^8.0.1"
  }
}
